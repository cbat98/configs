{ config, pkgs, ... }:

{
  # Currently empty as theme settings were mostly env vars in Hyprland or waybar CSS.
  # Can be populated with gtk/qt settings later.
  # For now, just holding the space.
}
